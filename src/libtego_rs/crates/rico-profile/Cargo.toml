[package]
name = "rico-profile"
authors = ["morgan@blueprintforfreespeech.net"]
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"
description = "A library for reading+writing Ricochet-Refresh profile files"
repository = "https://github.com/blueprint-freespeech/ricochet-refresh"

[dependencies]
anyhow = { version = "1.0", optional = true }
rico-protocol = { version = "0.1.1", path = "../rico-protocol", features = ["rusqlite-traits"]}
rusqlite = { version = "0.38", features = ["bundled-sqlcipher-vendored-openssl"], optional = true }
serde = { version = "1.0", features = ["derive"], optional = true}
serde_json = { version = "1.0", optional = true}
thiserror = "2.0"
time = "0.3"
tor-interface = {version = "0.6.8", features = ["legacy-tor-provider"]}

[dev-dependencies]
anyhow = "1.0"
serde_json = "1.0"

[features]
test-features = ["anyhow"]
v3-profile = ["serde", "serde_json"]
v4-profile = ["rusqlite"]

[[test]]
name = "v3"
required-features = ["v3-profile"]

[[test]]
name = "v4"
required-features = ["v4-profile", "test-features"]
